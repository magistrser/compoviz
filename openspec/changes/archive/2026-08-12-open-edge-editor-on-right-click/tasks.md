## 1. Interaction Regression

- [x] 1.1 Update the `VisualBuilder` interaction harness and add regression coverage proving right-click opens the existing dependency-condition popup while double-click does not.

## 2. Edge Interaction

- [x] 2.1 Replace dependency-edge double-click editing with context-menu editing, suppressing the native menu and preserving Enter, selection, deletion, and non-dependency behavior.

## 3. Validation

- [x] 3.1 Run the focused interaction test, project quality gates, production build, strict OpenSpec validation, and a browser smoke check.

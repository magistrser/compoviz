## 1. Regression Coverage

- [x] 1.1 Add dependency-edge tests for the three condition colors, label removal, fallback behavior, and selected emphasis.
- [x] 1.2 Add a builder test for the grouped Depends On legend and its three human-readable condition keys.

## 2. Builder Presentation

- [x] 2.1 Add a shared typed dependency-condition palette and use it to render label-free dependency edges.
- [x] 2.2 Redesign the Connection Types legend and styles around a compact dependency-condition group.

## 3. Validation

- [x] 3.1 Run the focused edge and builder component tests.
- [x] 3.2 Run the repository quality gates and strict OpenSpec validation.
- [x] 3.3 Inspect the Build view in the browser and correct any visual regressions.
